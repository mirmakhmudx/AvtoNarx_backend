; AvtoNarx uchun maxsus PHP sozlamalari.
; RunParserSourceJob katta HTML sahifalarni (DOM Crawler) ketma-ket
; qayta ishlaydi — standart 128M yetarli emas, "Allowed memory size
; exhausted" xatosiga olib keladi (parser_targets soni o'sgani sayin
; muammo kuchayadi). CLI/FPM/queue worker — barchasi shu faylni o'qiydi.
memory_limit = 512M
